import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    margin: 20
  },
  label: {
    fontSize: 15,
    marginTop: 5,
    fontWeight: "normal"
  },
  view: {
    position: "absolute"
  }
});
