import { StyleSheet } from "react-native";

// todo unused
export default StyleSheet.create({
  home: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center"
  },
  image: {
    width: 50,
    height: 50
  }
});
